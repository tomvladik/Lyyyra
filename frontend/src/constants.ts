// Timing constants for debouncing and polling
export const DEBOUNCE_DELAY = 500; // ms - Delay for search input debouncing
export const STATUS_POLL_INTERVAL = 500; // ms - Polling interval for status updates during progress
export const SONG_POLL_INTERVAL = 2000; // ms - Polling interval for song list updates during DB fill
export const INITIAL_LOAD_DELAY = 500; // ms - Initial delay before loading data on component mount
export const FETCH_STATUS_DELAY = 600; // ms - Delay before fetching status in InfoBox
